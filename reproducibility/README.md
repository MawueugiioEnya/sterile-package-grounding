# Reproducibility record

`pipeline_sha256.txt` contains SHA256 checksums recorded for the code used
during the publication processing run on 18 August 2026.

The corresponding source files were independently re-hashed before creation
of this repository and matched the recorded values.

The original processing environment is documented in `environment.txt`.

The public repository may contain portability changes to launcher scripts,
such as removal of cluster-specific absolute paths. Such changes do not alter
the grounding algorithms used to produce the reported results.
